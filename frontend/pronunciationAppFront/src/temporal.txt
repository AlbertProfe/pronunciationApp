<Box
      sx={{
        p: 2,
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
      }}
    >
      {users
        .filter((user) => user.id === 1)
        .map((user) => (
          <Card
            key={user.id}
            sx={{
              maxWidth: 300,
              bgcolor: "grey.900",
              color: "common.white",
              overflow: "hidden",
            }}
          >
            <Box
              sx={{
                height: 100,
                background:
                  "linear-gradient(135deg, #2196f3 0%,rgb(172, 209, 203) 100%)",
              }}
            >
              <Box
                sx={{
                  display: "flex",
                  flexDirection: "row",
                  alignItems: "center",
                  justifyContent: "center",
                }}
              >
                <Typography variant="h6" sx={{ color: "black" }}>
                  Welcome to Pronunciation App {user.username}
                </Typography>
                <Avatar sx={{ bgcolor: "#1769aa" }}>
                  {Array.from(user.username)[0]}
                </Avatar>
                <Typography
                  variant="h4"
                  component="h4"
                  sx={{
                    fontWeight: "bold",
                  }}
                >
                  {user.username}
                </Typography>
              </Box>
            </Box>
            <CardContent>
              <Box sx={{ display: "flex", flexDirection: "column", gap: 1 }}>
                <Box sx={{ display: "flex", alignItems: "center", gap: 1 }}>
                  <Typography variant="body2" color="grey.400">
                    ID:
                  </Typography>
                  <Typography variant="body2" color="grey.500">
                    {user.id}
                  </Typography>
                </Box>
                {user.level && (
                  <Box sx={{ display: "flex", alignItems: "center", gap: 1 }}>
                    <Typography variant="body2" color="grey.400">
                      Level:
                    </Typography>
                    <Typography variant="body2" color="grey.500">
                      {user.level}
                    </Typography>
                  </Box>
                )}
              </Box>
            </CardContent>
          </Card>
        ))}
    </Box>